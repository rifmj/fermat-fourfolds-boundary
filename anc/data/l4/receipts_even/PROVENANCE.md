# Even-sector receipts (pinned historical runs)

These are the verbatim logs of the campaign runs behind the even-degree section's
computer-assisted statements (Propositions on the even census and wall structure; the deep
negative sweeps quoted in the manuscript). They are PINNED HISTORICAL ARTIFACTS — checksummed in
SHA256SUMS — not regenerated by run_all.sh (several represent multi-hour negative sweeps).

- census_even.log            — even-parity census engine log (m<=108 tier and calibrations)
- 210_exchange_full.log      — the exhaustive exchange scan at degree 210
- exchange_close.log         — exchange-relation runs (wall adjacencies: 110 pair, 114 triangle,
                               isolation of 70/168)
- {70,110,114}_gen2_d4.log, 70_gen2_d5.log, 110_pair_d4.log, 114_triple_d4.log
                             — generation-2 depth-4/5 NO-FIRE negative sweeps on the wall members
- 114_triple_lift228.log, 168_lift504_d2.log, e3e5_lifts_d2.log
                             — level-lift NO-FIRE sweeps
- 168_1_d5.log               — the m=168 depth-5 own-level exhaustion
- D220_d3.log, 210_new_d3.log, 210_new_d4.log — the degree-220/210 class sweeps

The complete fresh even census (v3 engine with ledger transport, all even 6 <= m <= 250) is the
sibling artifact ../census_receipt_even6_250.txt with a single replay command in the README.
Deep-closure witnesses for the even sector are the routes of ../CLOSED_LEDGER.tsv; the ten open
primitives carry independent modular kernel witnesses (runner step 9).

## 2026-07-28 — companion referee round r1 (Sol) receipt additions
Generated by the SHIPPED engines of this package (no new code):
- `exchange_lift140.log`, `exchange_lift220_c{1,2,3}.log`, `exchange_lift228_c{1,2,3}.log` —
  exhaustive exchange scans (both |A| shapes) at the lifted levels 140/220/228: the 140 lift is
  isolated; the three 220 classes and the three 228 lifts each form a complete clique; ZERO links
  to closed classes anywhere (`exchange_close.py`, custom target + open-set mode; open sets = the
  census's open rows at those levels).
- `w168_xi28_congruence.txt` — asserted receipt for the W168 Proposition's lifted congruence
  u(a) == u(6*xi_28) mod S_168, with non-vacuity sanity (both vectors individually OUTSIDE S_168);
  re-derivable via `s_lattice_core` (the script is embedded in the receipt header).

## 2026-07-28 — external round: independent even check + self-pair integrity receipts
- `even_independent_key_levels.txt` — the independent reimplementation run fresh at 50/70/110/114/168
  AFTER its even-m multiplicity fix (its original decomposability was set-based — unsound at even m,
  a single m/2 read as a vanishing pair; regression class (1,29,35,43,45,57)@70). Representative
  counts AND base-closure counts now match the pinned engine receipt exactly (5/5). Corrected
  attribution: the receipt engine (census_scan_v3 -> census_even) is even-safe by construction; the
  earlier mismatch was the independent script's set bug, not the engine. (The odd-era display engine
  census_scan_v2 separately omits the self-pair in its quasi loop — display-tally only, no receipt
  rests on it.) A genuine self-pair-quasi witness is pinned.
- `walls_selfpair_quasi_exhaustive.txt` — exhaustive exact quasi test (every pair INCLUDING the
  self-pair, every 4+4 split, all units) of ALL TEN residual-table classes: 0 witnesses everywhere —
  no wall collapses; plus the documented tier-discrepancy witness.
